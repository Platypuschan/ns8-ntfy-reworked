{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Get ntfy settings",
    "$id": "http://nethserver.org/json-schema/task/input/ntfy/get-configuration",
    "description": "Get ntfy settings",
    "examples": [
        {
            "host": "ntfy.domain.org",
            "http2https": true,
            "lets_encrypt": true,
            "server_config": "base-url: https://ntfy.domain.org\n"
        }
    ],
    "type": "object",
    "required": [
        "host",
        "http2https",
        "lets_encrypt",
        "server_config"
    ],
    "properties": {
        "host": {
            "type": "string",
            "description": "Host name for the application, like 'ntfy.domain.org'",
            "format": "idn-hostname"
        },
        "lets_encrypt": {
            "type": "boolean",
            "title": "Let's Encrypt certificate",
            "description": "Request a valid Let's Encrypt certificate."
        },
        "http2https": {
            "type": "boolean",
            "title": "HTTP to HTTPS redirection",
            "description": "Redirect all the HTTP requests to HTTPS"
        },
        "server_config": {
            "type": "string",
            "title": "ntfy server.yml",
            "description": "Complete ntfy server configuration in YAML format"
        }
    }
}
